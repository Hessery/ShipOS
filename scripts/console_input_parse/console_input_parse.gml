// Makes sure the inputed string is all caps and short

function console_input_parse() {
	
	keyboard_string = string_upper(keyboard_string);
	keyboard_string = string_copy(keyboard_string, 1, 80);
	
}